# 2.0 ラン状態(生きた文書 — 統括が常に最新化する)

> セッション断絶からの回復手順: このファイルと `release-2.0-scope.md` を読む → 「進行中」の項目を確認 → 未完了ブランチ/PRの状態を `gh pr list` と `git branch -a` で照合 → 走行を再開。ユーザーへの中間確認は不要(契約済み)。

## 現在のフェーズ

- **P1 描画コア**: 進行中(ブランチ agent/v2-render-core)
- **P2 アニメ基盤**: 本パイプライン化 進行中(スパイク成立済み)
- **ルック開発(渋く重厚トーン)**: 進行中(補足合意で確定した新トーンの実レンダー比較)

## 進行中の作業

| 作業 | ブランチ | 状態 |
|---|---|---|
| 60fps ticker + 補間 + 保持型シーングラフ + マップ初回送信化 | agent/v2-render-core | エージェント実装中 |
| アニメパイプライン本実装(スキーマ+asset-tools多フレーム+足軽品質引き上げ) | agent/v2-anim-pipeline-prod | エージェント実装中 |
| 渋く重厚トーンのルック開発(グレード3案の実レンダー比較) | (スパイクのみ、本番非改変) | エージェント実装中 |

## 完了済みマイルストーン

- 2026-07-07: v1.0 タグ凍結(667cb5a)、2.0スコープ合意・文書化
- 2026-07-07: 補足合意4件(カメラ固定/架空の城/サウンドなし/**トーン=渋く重厚**)を scope に追記
- 2026-07-07: **P2スパイク成立**(agent/v2-anim-pipeline, 0502b05)。リグ+キーフレーム→8方向シート方式を採用確定。実測0.028s/フレーム、全量産1,536枚≒1分強、律速はレンダーでなくモデリング工数。品質は既存Blender系と同質(量産時に部位分割+ベベルで引き上げ)。残リスク: 騎馬(四足歩様)・荷車はヒト型リグ流用不可

## 次のアクション(キュー)

1. P1 PR のレビュー・E2E裏取り・マージ → 直後に P4 sim側(elevation型・A*・崖坂)レーン起動
2. ルック開発の3案比較 → トーン確定 → パイプラインのポストプロセス(トーン較正段)として全アセット適用
3. アニメパイプライン本実装のPRレビュー → マージ → P1マージ後にクライアント再生系(状態機械)
4. P2完了後: P3量産(6種×4アクション×8方向、新トーン適用後に実施)
5. マイルストーンごとに非ブロッキング報告(画像付き)を出す

## 運用メモ

- コミット規約: `v$(date +%y%m%d%H%M)` スナップショット、PRはCI green条件付きマージ
- E2E はポート5179専有 → 直列実行。ゾンビ vitest は `pkill -f "[v]itest"`
- 実画面スクショはシーン構築 ~15秒待ち必須(waitForTimeout(18000))
- Blender: `ASAMA_BLENDER_BIN=/nix/store/s0jp4xvpkzc3j00xf7m4d5j385k487lj-blender-5.1.1/bin/blender`
- registry.py を変更すると全レンダーキャッシュ無効化 → 触るときは覚悟を決めてから
- 上限例外: codex/claude のサブスク上限に達しそうな場合のみユーザーに相談(それ以外は止まらない)
