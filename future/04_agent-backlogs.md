# 各エージェントの初期バックログ(2026-07-05 現状反映版)

優先順位付きの初期案。各エージェントは先頭から着手する。
確定した仕様は `docs/` へ昇格し、本ファイルからは削除する。

> 2026-07-05更新: ペインタリー移行・植生・建物ペイントオーバー・地形マクロ化・
> 堀トレンチ化・ミニマップ等QoLは**完了済み**のため削除。
> アート制作の標準工程は「3D構図(supersample 8)→codexペイントオーバー→
> 取り込みゲート(トーン較正+アルファマスク)」に確定(art-direction.md参照)。

## アートA(建築・地形)

1. **[基盤] `render_asset.py` のモジュール分割**(→ `03_prerequisites.md`。
   **キャッシュキーのモジュール別化まで含めて完了**)
2. **[基盤] `production-assets.json` のカテゴリ別分割**
3. 大型建物 footprint 整合の根本解決
   (2026-06-22 の補正は既存raster保持の暫定対応で、再制作が未完)
4. 天守の置き換え判定と実施(**再スコープ済み**: 手続きモデリングではなく
   「3D構図トレース+ペイントオーバー」工程で。現AIアート維持との比較判定はユーザーゲート)
5. 接続キット(塀・柵・門・橋)の質感向上検討
   (ペイントオーバーはマスク間の継ぎ目契約があるため直接適用不可。
   手続きマテリアル改良 or マスク別ペイントオーバー+境界検証の工程開発)
6. 土塁・石垣のアセット化(地形/建造物の表現方式が未確定 →
   `docs/10_development/unresolved-issues.md`。シミュレーションエージェントと要調整)
7. タイルオブジェクト全般の「角の丸み」原則の展開(道の円形パッドで実証済み。
   塀・柵の角への適用検討)

## アートB(ユニット・植生)

1. ユニットの4方向表示・アニメーション(現状 idle 3種のみ。最大の空白地帯。
   ペイントオーバー工程のアニメ適用方法=フレーム間の筆致一貫性の確立を含む)
2. 既存兵種(槍足軽・刀足軽・弓・工兵)の move / attack フレーム
3. 新兵種スプライト: 鉄砲・騎兵・補給荷車
   (**シミュレーションエージェントのユニット仕様確定待ち**。requests で受領)
4. 建物破壊状態スプライト(アートAの建築モデルに依存。要調整)
5. 竹のペイントオーバー版検討(現状唯一の手続き版植生。現状で承認済みのため低優先)
6. 季節バリエーション(紅葉・雪)の検討(未確定)

## シミュレーション

1. **[基盤] `index.ts` のモジュール分割**(→ `03_prerequisites.md`)
2. 攻撃側兵糧(補給荷車ベース)と攻撃側兵糧切れ敗北
   (勝敗構造の片翼。ゲームコンセプト上の欠落のため最優先の機能)
3. 兵種相性・高低差補正・射線(MVP繰り延べ項目)
4. 新兵種: 鉄砲兵・騎兵・補給荷車(仕様確定後、アートBへ requests で連絡)
5. 土塁・石垣のルール実装(表現方式の未解決事項の解消。アートAと要調整)
6. 火災システム(`docs/03_combat/fire-system-future.md` に将来仕様あり)
7. 木材・石材資源と建築コスト(2026-07-04時点で見送り中の未解決事項)

## コンテンツ/AI

1. AIプロファイル追加: 梯子強襲・堀埋め・混合型
   (MVPスコープ記載だが現状「正面強襲」1種のみ。
   必要なエンジンAPIは requests でシミュレーションへ依頼)
2. シナリオ複数化(現状 `mvpDefenseScenario` 1本。シナリオシステムはデータ駆動済み。
   `tools/scene-preview/` のレイアウト定義がシナリオ下書きに転用可能)
3. バランス調整値のデータ整備
   (`FOOD_BALANCE` / `ECONOMY_BALANCE` / `SIEGE_BALANCE` 等の暫定値の検証と調整)
4. 天守様式のコンテンツ定義(`docs/04_content/tenshu-styles.md`。アートAの天守制作と連動)

## UI/基盤

1. **[基盤] `GameCanvas.tsx` のレイヤー分割**(→ `03_prerequisites.md`)
2. 選択情報パネル(MVP繰り延べ項目。ミニマップ・警報・セーブスロットは実装済み)
3. 操作系の回収: H位置固定 / ダブルクリック同兵種選択 / グループ登録
4. UI層のテスト整備(現状 apps/game 側テストゼロ)
5. ズームレベル別のアセット表示検証(ペイントオーバー版の1x/縮小表示の見え方確認)

## 凡例

- **[基盤]** = `03_prerequisites.md` の直列整備タスク。同領域の機能追加より先に完了させる
- 太字の依存関係は `01_agent-split.md` の「既知の依存関係」を参照
