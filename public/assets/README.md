# Runtime Assets

Generated assets used by the game client are written here.

Run:

```bash
pnpm run generate:assets
pnpm run validate:assets
```

The application should resolve images through `public/assets/placeholders/manifest.json` during the initial placeholder phase.
