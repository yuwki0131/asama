# Asset Source

This directory contains source definitions for generated assets.

- `placeholder-assets.json`: deterministic placeholder image definitions used before the Blender pipeline exists.
- Generated PNGs are written to `public/assets/placeholders/`.
- Do not manually edit generated files; update source definitions and rerun `pnpm run generate:assets`.
