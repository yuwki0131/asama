"""Isolated SHORE registry (terrain water shore/transition tiles).

Split from the static terrain.py registry (trench/fence/machiya precedent)
so the pale-rim fix (patrol ledger V-06 白茶け) re-renders only the 70
terrain-water shore assets instead of invalidating the full render cache.
"""
