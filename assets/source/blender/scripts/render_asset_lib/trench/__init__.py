"""Isolated TRENCH registry (moat/river families).

Split from the static terrain.py registry (fence/machiya precedent) so the
water-repetition fixes — no holdout shadow bands, readable phase field —
re-render only the moat/river assets instead of invalidating the full
render cache.
"""
